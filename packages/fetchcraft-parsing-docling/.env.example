# Server Configuration
HOST=0.0.0.0
PORT=8080

# Concurrency Configuration
MAX_CONCURRENT_REQUESTS=10  # Maximum number of concurrent requests
MAX_CONCURRENT_FILES=4       # Maximum number of files processed in parallel

# File Upload Configuration
MAX_FILE_SIZE_MB=100         # Maximum file size in MB

# Docling Parser Configuration
PAGE_CHUNKS=true             # Split documents into separate pages
DO_OCR=true                  # Enable OCR for scanned documents and images
DO_TABLE_STRUCTURE=true      # Extract table structure
