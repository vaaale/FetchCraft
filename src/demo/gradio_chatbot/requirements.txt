# Gradio Chatbot Requirements

# Web UI
gradio>=4.0.0

# Core dependencies (should already be installed)
# These are listed in the main project requirements
# qdrant-client
# pydantic-ai
# openai
# fastembed  # Required for hybrid search

# Optional: For better performance
# uvloop  # Faster async event loop (Unix only)
